{
  "name": "@fitvo/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "db:seed:exercises": "tsx src/seed/exercise-library/seed-exercise-library.cli.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "build": "prisma generate",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "@fitvo/eslint-config": "workspace:*",
    "@fitvo/typescript-config": "workspace:*",
    "@types/node": "^26.2.0",
    "eslint": "^10.8.1",
    "prisma": "^6.1.0",
    "tsx": "^4.23.12",
    "typescript": "^5.6.2",
    "vitest": "^4.1.10"
  }
}
