# URL base da API FITVO (backend Fastify), com o prefixo de versao /v1.
# Consumida SO no servidor (BFF / route handlers) — sem prefixo NEXT_PUBLIC_,
# entao NAO vai para o bundle do cliente. Placeholder, sem segredo.
API_URL=http://localhost:3333/v1
