# Variaveis do docker-compose de desenvolvimento.
# Copie para docker/.env (ignorado pelo git) e ajuste conforme necessario.
# Uso local apenas — nunca versionar valores reais.

POSTGRES_USER=fitvo
POSTGRES_PASSWORD=change-me
POSTGRES_DB=fitvo
POSTGRES_PORT=5432

REDIS_PORT=6379
